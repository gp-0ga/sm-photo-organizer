# 図面数量拾いMVP 引き継ぎメモ

## 作業フォルダ

`C:\Users\gpt0g\Documents\ChatGPT\SM業務`

## 起動

```powershell
npm install
npm run dev
```

ブラウザで `http://127.0.0.1:5173/drawing.html` を開く。

ローカル処理前提。PDFレンダリングはブラウザ内のPDF.js(pdfjs-dist)で行い、図面データは外部送信しない。サーバー側の変換処理(poppler)は不要になり、`npm run build`で生成した静的ファイル一式だけで動作する(社内サーバーへの配置やPCへのコピー配布が可能)。

スキャン系PDF(JBIG2圧縮の白黒画像を含むもの、コピー機経由でPDF化した図面など)を正しく画像化するため、`pdfjs-dist`のJBIG2/OpenJPEG等のWASMデコーダを`public/pdfjs-wasm/`に配置し、`getDocument()`へ`wasmUrl`を明示指定している。これがないと該当ページが白紙のまま表示される不具合が起きるため、`pdfjs-dist`を更新した際は`node_modules/pdfjs-dist/wasm/`の中身を`public/pdfjs-wasm/`へ再コピーすること。

## 主要ファイル

- `drawing.html`
- `src/drawing-main.js`
- `src/drawing.css`
- `vite.config.js`

## 現在できること

- PDFをローカルでPNG化して複数ページ表示する。
- ページ送り、左ペインのページカード選択、ズーム、ドラッグ移動ができる。
- 図面種別をページごとに設定できる。
  - 平面図
  - 立面図
  - 屋上平面図
  - 断面図
  - 矩計図
  - 面積表
  - 使わない
- 図面ごとに縮尺を確定できる。
- 平面図などの既知寸法2点をクリックして補正係数を算出できる。
- 補正係数は手入力で丸めて採用できる。
- 同じ縮尺、または平面図・立面図へ補正係数を適用できる。
- 数量拾いは多角形、四角形、線数量に対応。
- Shift/Ctrlで水平・垂直スナップできる。
- 四角形はドラッグ作図できる。
- 確定済み数量を一覧表示し、選択、削除、属性編集、手修正値の入力ができる。
- Excel出力は `数量表`、`根拠一覧`、`集計` の3シート。
- Excel出力時は棟、立面方位、立面範囲、仕上、分類、図面名の順で並べる。
- 開口は面積と周長を出力する。
- 外壁面積、開口控除から外壁差引と開口率を計算する。
- 外壁面積を外部足場概算面積、建物周長を安全手すり概算延長としてExcelへ概算転用する。
- 作業はブラウザ内IndexedDBへ保存、読み込み、削除できる。

## ユーザー要望として残っていること

- 作業保存をブラウザ内だけでなく、PCファイルとして保存・復元できるようにする。
  - 候補: 作業JSONをエクスポート、インポート。
  - 将来は図面画像も含むZIP保存が望ましい。
- TIFF直接読み込み。
- DocuWorks直接読み込みは現時点では未対応。まずはDocuWorks側でPDF/TIFF化する運用が現実的。
- OCR/AI補助は未実装。まずは候補検出ではなく、人が拾った数量の根拠と補正確認を強くする方針。
- 立面図にも既知寸法による縮尺確認を入れるか検討。
- 面積表図面から棟ごとの面積や寸法情報を読み取る導線。
- 複数棟、仕上げ別、北面/南面/西面などの入力補助。
  - 方位は標準選択しつつ直接入力できるUIが望ましい。
- 四角形編集では、頂点単体移動ではなく四角形として幅・高さを保つ編集もほしい。
- 作図中や確定後に、幅 x 高さ、面積、周長を画面上に表示し、13.01mを13.00mへ丸めるような補正入力をしやすくする。
- Excel集計は今後さらに棟、仕上、方位、分類ごとの見せ方を調整する。

## 検証済み

```powershell
npm run build
npm test
```

2026-09-13時点でどちらも成功。

## 注意

このリポジトリは元々「資産写真整理MVP」のプロジェクトで、そこへ `drawing.html` と図面数量拾い機能を追加している。図面数量拾いだけを独立プロジェクト化するなら、上記主要ファイルと `package.json` の `xlsx` 依存、`vite` 開発環境を切り出す。
